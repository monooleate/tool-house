---
import ToolLayout from "../../layouts/ToolLayout.astro";
import JsonFormazasTool from "../../components/tools/fejleszto/JsonFormazasTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";
const tool = getToolBySlug("fejleszto", "json-formazas")!;
---
<ToolLayout tool={tool}>
  <JsonFormazasTool client:visible />
</ToolLayout>
