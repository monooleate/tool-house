---
import ToolLayout from "../../layouts/ToolLayout.astro";
import CodeFormatterTool from "../../components/tools/shared/CodeFormatterTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("fejleszto", "xml-ellenorzes")!;
---

<ToolLayout tool={tool}>
  <CodeFormatterTool client:visible mode="validate" language="xml" placeholder="Illeszd be az XML kódot..." />
</ToolLayout>
