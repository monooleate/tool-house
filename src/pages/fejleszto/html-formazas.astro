---
import ToolLayout from "../../layouts/ToolLayout.astro";
import CodeFormatterTool from "../../components/tools/shared/CodeFormatterTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("fejleszto", "html-formazas")!;
---

<ToolLayout tool={tool}>
  <CodeFormatterTool client:visible mode="format" language="html" placeholder="Illeszd be a HTML kódot..." />
</ToolLayout>
