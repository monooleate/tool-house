---
import ToolLayout from "../../layouts/ToolLayout.astro";
import KarakterCsereTool from "../../components/tools/szoveg/KarakterCsereTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("szoveg", "karaktercsere")!;
---

<ToolLayout tool={tool}>
  <KarakterCsereTool client:visible />
</ToolLayout>
