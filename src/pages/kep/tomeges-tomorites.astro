---
import ToolLayout from "../../layouts/ToolLayout.astro";
import TomegesTomoritesTool from "../../components/tools/kep/TomegesTomoritesTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("kep", "tomeges-tomorites")!;
---

<ToolLayout tool={tool}>
  <TomegesTomoritesTool client:visible />
</ToolLayout>
