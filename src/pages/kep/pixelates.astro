---
import ToolLayout from "../../layouts/ToolLayout.astro";
import PixelatesTool from "../../components/tools/kep/PixelatesTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("kep", "pixelates")!;
---

<ToolLayout tool={tool}>
  <PixelatesTool client:visible />
</ToolLayout>
