---
import ToolLayout from "../../layouts/ToolLayout.astro";
import LevagoTool from "../../components/tools/kep/LevagoTool.svelte";
import { getToolBySlug } from "../../lib/tool-registry.ts";

const tool = getToolBySlug("kep", "levagas")!;
---

<ToolLayout tool={tool}>
  <LevagoTool client:visible />
</ToolLayout>
